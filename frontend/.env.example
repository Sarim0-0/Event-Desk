# The browser calls this URL. Keep /api locally to use Vite's same-origin proxy.
VITE_API_BASE_URL=/api

# Local FastAPI address used by the Vite development proxy.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
